/* 
 * File:   Settings.hpp
 * Author: tku
 *
 * Created on 17 February 2014, 17:31
 */

#ifndef SETTINGS_HPP
#define SETTINGS_HPP

/**
 * This file contains preprocessor definitions which are used in graphics engine.
 */

/**
 * Version of the current compilation
 */
#define NGE_MAJOR_VERSION @MAJOR_VERSION@
#define NGE_MINOR_VERSION @MINOR_VERSION@
#define NGE_PATCH_VERSION @PATCH_VERSION@
#define NGE_COMMIT_HASH @COMMIT_HASH@

/**
 * When this flag is set, additional methods in various classes are available.
 * It is used to debug engine - get additional information. It should not be
 * used in production.
 */
#ifndef NGE_DEBUG
#define NGE_DEBUG
#endif

/**
 * When this flag is set to true, the engine needs libjpeg library to compile.
 * libjpeg is used for JPEG and JPG image loader (JpegImage class)
 */
#ifndef NGE_LIB_JPEG
#define NGE_LIB_JPEG true
#endif

/**
 * Rendering API - it can be OpenGL, OpenGL ES or DirectX.
 */

#endif /* SETTINGS_HPP */

